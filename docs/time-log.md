# Time log

Core budget: 300 minutes (Phases A–C). Phase D is outside the budget by design.

| Date | Task | Minutes | Running total | Notes |
|---|---|---|---|---|
| 2026-09-17 | 1 API scaffold, settings, /healthz | 20 | 20 | |
| 2026-09-17 | 2 DB layer, models, Alembic | 35 | 55 | |
| 2026-09-17 | 3 industry map + Overpass QL | 12 | 67 | |
| 2026-09-17 | 4 geocode + cache | 18 | 85 | fixed sync/async context-manager bug in brief's test code |
| 2026-09-18 | 5 Overpass fetch, mirrors, cache | 22 | 107 | fixed sync/async context-manager bug in brief's test code again; fixed naive/aware datetime comparison bug (SQLite drops tzinfo on DateTime columns) in cache expiry check |
| 2026-09-18 | 6 normalize, dedupe, chain flag | 26 | 133 | fixed a bug in brief's `normalize_name`: dotted suffixes like "L.L.C." split into single-letter tokens ("l","l","c") on the punctuation regex, but the suffix-filter set only stripped leading/trailing dots, so it never matched; stripped dots from both the input and the suffix set before tokenizing |
| 2026-09-18 | 7 crawler + text cleaning | 12 | 145 | brief's fixture was too short for the "good" quality threshold; added two neutral filler paragraphs (services/insurance/hours/new-patients) to `site_home.html` per controller ruling — no code bugs found, brief's `crawl.py` passed all 4 tests unmodified except one E501 docstring line |
| 2026-09-18 | 8 regex extraction | 20 | 165 | removed `footer` from `STRIP_TAGS` per controller ruling (footers carry © years/addresses) — all 5 crawl tests still passed unchanged; brief's regexes and extraction logic passed all 3 tests unmodified on first run, no bugs found |
| 2026-09-18 | 9 verification | 13 | 178 | brief's `verify.py` (MX lookup, disposable list, phone validity) passed all 3 tests unmodified on first run, no bugs found |
| 2026-09-18 | 10 scoring engine + golden fixture | 17 | 195 | hand-recomputed all 4 golden cases against the factor logic before writing the fixture — all matched the brief's JSON exactly, no arithmetic discrepancies; removed an unused `FactorScore` import from the brief's test file (ruff F401), split all `pts += n; why.append(...)` one-liners in `score.py` onto two lines (ruff E702); brief's scoring logic passed all 8 tests unmodified on first run, no bugs found |
| 2026-09-18 | 11 Groq client, throttle, schemas | 12 | 207 | (resumed after crash) — prior implementer's uncommitted draft files matched the brief exactly (only ruff-formatted for line length; no logic divergence); confirmed installed `groq==0.37.1` `RateLimitError`/`APIStatusError` signature matches the test helper's `response=`/`body=` kwargs unmodified; 7/7 llm tests + 48/48 full suite pass, ruff check/format clean, no bugs found |
| 2026-09-18 | 12 LLM extraction stage + merge | 11 | 218 | brief's `extract_llm.py` (gating, relevant-text prioritisation, validated merge policy) passed all 4 tests unmodified except ruff-format wrapping the long `RELEVANT` regex line; 4/4 new tests + 55/55 full suite pass, ruff check/format clean, no bugs found |
| 2026-09-18 | 13 event bus, schemas, build_facts | 6 | 224 | brief's `events.py` and `schemas.py` (replay buffer bus, output schemas, `build_facts`) passed all tests unmodified except ruff-format line wrapping; 4/4 new tests + 58/58 full suite pass, ruff check/format clean, no bugs found |
| 2026-09-18 | 14 pipeline runner | 16 | 240 | fixed 2 real defects per controller ruling: `LLMJob` was missing `osm_name` (brief passed the crawled page title into the LLM grounding check instead of the OSM listing name) and added `overpass_dentists_no_addr.json` so test 2's LLM address gap-fill isn't blocked by an OSM address already being set; brief's region-threading, `s.execute(delete(...))` calls, full-state `lead_updated` payload and delete-then-rewrite signal rows were already correct as written; 3/3 new tests + 62/62 full suite pass (3 consecutive runs, 0 warnings), ruff check/format clean |
| 2026-09-18 | 15 search/lead routers + SSE | 26 | 266 | applied controller ruling: made `create_search` `async def` (sync route + `asyncio.create_task` in `start_search_task` would raise `RuntimeError`); brief's routers otherwise passed all 5 new tests unmodified on first run (no `AppStatus` fixture needed, 3 consecutive full-suite runs green, 0 warnings); ruff flagged 2 things the brief didn't anticipate: `E501` from long verbatim lines (fixed by `ruff format .` per Step 7) and `B008` on every `Depends(get_session)` default (FastAPI's standard pattern, not exempted in this repo's ruff config) — fixed with per-line `# noqa: B008`, same precedent as the brief's own `# noqa: E731`, rather than a global rule disable; 68/68 full suite pass, ruff check/format clean |
| 2026-09-18 | 15 (fix pass) CORS-on-500, HTTPException MRO, SessionDep | 18 | 284 | post-review (measured against live uvicorn, not inspected) found `@app.exception_handler(Exception)` alone never gets CORS headers on a 500 — Starlette's `build_middleware_stack` pulls `Exception`/500 handlers onto `ServerErrorMiddleware`, outside every `add_middleware(...)` call; fixed with an `@app.middleware("http")` catch-all registered before `CORSMiddleware`, verified with a real uvicorn process + curl (500/404/405 all carry both the envelope and `access-control-allow-origin`); also: registered the HTTPException handler on `starlette.exceptions.HTTPException` so routing's own 404/405 get the envelope too; replaced 5× `# noqa: B008` with one `SessionDep = Annotated[Session, Depends(get_session)]`; replaced the `by = lambda` (`# noqa: E731`) in `leads_for` with a `defaultdict` pass; moved the SSE route's existence check into an explicit `with session_scope()`; hardened `deps.shutdown()` to `gather` cancelled tasks before closing the HTTP client; deleted dead `request_settings`; wired `settings.max_leads_per_search` as a real clamp in `create_search` (new test, RED confirmed then GREEN); 69/69 full suite pass (3 consecutive runs, 0 warnings), ruff check/format clean, zero `noqa: B008`/`E731` remain in `app/` |
| 2026-09-18 | 16 CSV + HubSpot export | 14 | 298 | brief's `export.py` (generic CSV with `#`-comment ODbL attribution line, HubSpot CSV with attribution in a trailing `Data Sources` column, weights-driven rescoring via `pipeline.score.total`/`tier`) and `routers/export.py` passed all tests unmodified on first run, no bugs found; used `s: SessionDep` per controller ruling instead of the brief's `Depends(get_session)` default; 4/4 new tests (3 service + 1 router) + 73/73 full suite pass (3 consecutive runs, 0 warnings), ruff check/format clean, no new `noqa` in `app/` |
| 2026-09-18 | 16 (fix round 1) real weight labelling + BOM | 15 | 313 | corrects this row's "no bugs found" — review found 3 Important defects inherited from the brief's own code: (1) `_weights_str(weights or {})` labelled every default export `0/0/0/0/0` instead of the search's real preset while the score column stayed correct, a false-attribution bug in spec §15's own text — fixed by splitting labelling from recomputation: the route now resolves `effective = override or WEIGHT_PRESETS.get(search.weight_preset, ...)` and passes an explicit `recompute` flag through `to_csv`/`to_hubspot_csv`/`_rescore` so weights are always a real dict and only get applied to the score when an override was actually supplied (RED confirmed via `git stash` of the source fix against the new test — captured output literally showed `weights R/E/D/B/S=0/0/0/0/0`); (2) the positional `dict(zip(FACTORS, ...))` in `parse_weights` had no integration coverage — added a router test with per-factor points that make transposition detectable (`weights=100,0,0,0,0` -> 100.0, `weights=0,0,0,0,100` -> 0.0); (3) the generic (Excel-facing) CSV had no UTF-8 BOM, so Excel's ANSI-codepage fallback mangles the "©"/"·" attribution text on Windows — fixed with a `BOM = chr(0xFEFF)` prefix on the generic export only (HubSpot stays BOM-free since a BOM can mangle its importer's first header); 3 new tests (1 service + 2 router) + 76/76 full suite pass (3 consecutive runs, 0 warnings), ruff check/format clean, no new `noqa` in `app/` |
| 2026-09-18 | 19 web scaffold, types, client, rank.ts (resumed after crash) | 25 | — | confirmed prior agent's scaffold already had react/react-dom/@tanstack/react-table/lucide-react/@types/node installed; installed missing tailwindcss/@tailwindcss/vite/vitest; this create-vite flavor's scaffold noise differs from the brief's assumed file names (no App.css/react.svg/public vite.svg — instead hero.png/vite.svg/icons.svg/favicon.svg), so only `src/index.css` existed to delete; fixed `ApiError` constructor parameter-property shorthand, which the scaffold's `erasableSyntaxOnly` tsconfig flag rejects, by declaring fields explicitly instead of weakening the flag; brief's `rank.ts`/`rank.test.ts` (with controller's `node:fs` golden-fixture ruling) passed all 6 tests unmodified on first run, no scoring bugs found |
| 2026-09-18 | 20 session reducer, SSE, polling hook | 12 | — | scaffold has no ESLint config (lint script uses oxlint), so per controller ruling omitted the brief's `// eslint-disable-line react-hooks/exhaustive-deps` comment (an unknown-rule directive would itself be a lint issue); used `<T>` not `<T,>` in `sse.ts`'s generic arrow since it's a `.ts` file, not `.tsx`; brief's reducer/sse/hook code passed all 9 tests unmodified on first run, no bugs found |
| 2026-09-18 | 21 app shell, search bar, status, empty state | 18 | — | added `import type React from "react"` in `SearchBar.tsx` for `React.FormEvent` (verbatimModuleSyntax); added the brief's `initialText?: string` prop (`useState(initialText ?? "")`) per controller ruling; added an `openId` state + clickable plain-list rows in `App.tsx` (not in the brief) per task instructions, so Task 22 has a hook to wire the drawer without an unused-locals violation; deleted scaffold's now-orphaned `src/assets/{hero.png,vite.svg}` and `public/icons.svg` (only referenced by the old scaffold `App.tsx`); set `index.html` `<title>` to `SquatchScout`; no runnable API in this lane, so verified via `pnpm dev` + Playwright instead of the brief's Step 5 (bootstrap fetches 502 against the dev proxy with no API running → `apiDown` true, retry button and empty state render, no uncaught JS exceptions); brief's component code passed typecheck/build/all 12 existing tests unmodified, no bugs found |
| 2026-09-18 | 22 results table + badges | 11 | — | installed `@tanstack/react-table@9.2.4` (Task 19) ships a rewritten v9 API — no `useReactTable`/`getCoreRowModel`/`getSortedRowModel`/2-generic `ColumnDef` on the main entry point; verified via scratch compile tests, then imported those four from the package's `/legacy` v8-compat subpath (`useLegacyTable as useReactTable`, `LegacyColumnDef as ColumnDef`) — same call shape as the brief, zero behavior change; added the header-checkbox `indeterminate` state via a ref callback per controller ruling 3; no `aria-pressed` on the row per ruling 1; verified sort-by-score-desc default, click-to-sort by Business/City, checkbox-click-doesn't-open vs row-click/Enter-does-open (via temporary mock leads + `onOpen` console.log, reverted before commit), no-results panel, streaming banner, and narrow-viewport (375px) internal card scroll with Playwright against `pnpm dev`; brief's component code otherwise unmodified; no bugs found in existing code; all 12 tests, typecheck and build pass |
| 2026-09-18 | 23 weights panel + summary tiles | 10 | — | brief's `WeightsPanel.tsx`/`SummaryTiles.tsx` used verbatim, both compiled and rendered on first try, no bugs found; wired the two-column `lg:grid-cols-[minmax(0,1fr)_320px]` rail into `App.tsx` only inside the non-idle branch (idle keeps the full-width `EmptyState`, matching the brief's snippet which replaces just the `<ResultsTable/>` placement); ruling 1 hygiene: added the `/legacy` import rationale comment to `ResultsTable.tsx`, and created `src/test-setup.ts` + `setupFiles` in `vite.config.ts` to fix the missing RTL `cleanup` registration (existing per-file `afterEach(cleanup)` left in place per ruling); verified live re-rank with no backend by temporarily seeding 3 fake leads with hand-picked factor scores into `searchReducer.ts`'s `initialState` (`phase: "done"`) and driving `pnpm dev` with Playwright — preset chips toggle `aria-pressed` and reorder the table with recomputed scores/tiers instantly, dragging a slider (arrow keys) updates its % label and reorders rows with only the sliders' own state changing (network log showed only the pre-existing `/healthz`+`/api/industries` bootstrap 502s, nothing new), "reset" re-applies `balanced`, and the rail collapses to a stacked column under 1024px (checked at 800px); reverted the scratch seed afterward (`git diff` on the file came back empty); all 15 tests, typecheck and build pass |
| 2026-09-17 | 24 lead drawer | 25 | — | measured; brief's `FactorBars`/`ContactsList`/`SignalsList`/`OpenerPanel`/`LeadDrawer` used verbatim, all compiled and rendered on first try, no bugs found; removed `void openId;` and mounted `<LeadDrawer lead={ranked.find((l) => l.id === openId) ?? null} .../>` in `App.tsx` per ruling 2 so `lead_updated` refreshes an open drawer in place; added one RTL test (`LeadDrawer.test.tsx`, dialog/aria-modal + Escape + factor reasons) confirmed RED (temporarily changed the Escape branch to `"NotEscape"`, saw the assertion fail) then GREEN after revert; verified live with `pnpm dev` + Playwright by temporarily seeding one fake lead with contacts/signals/factor_scores into `searchReducer.ts`'s `initialState` and forcing `useBootstrap`'s `llmEnabled` default to `true` (to exercise the Outreach section without a running API) — confirmed `role="dialog" aria-modal="true"`, focus moves into the panel on open and back to the triggering row on Escape, Tab from the last focusable element (`Draft call opener`) wraps to the first (`acmeplumbing.example` link) and Shift+Tab wraps back, clicking inside the panel does not close it while clicking the backdrop does, all five factor bars show points/max/weight%/reasons, contacts show mailto:/tel:/social links with status and source, signals show source pills; reverted both scratch edits afterward (`git diff` on `searchReducer.ts` and `useBootstrap.ts` came back empty); cross-checked task-17-brief.md's backend `opener()` handler to confirm the panel's "grounded only in the signals above" copy matches what the endpoint actually sends the LLM; all 17 tests (15 existing + 2 new), typecheck and build pass |
| 2026-09-18 | 25 export, toast, responsive, build | 28 | — | measured — Phase B done; brief's `ExportMenu.tsx`/`ErrorToast.tsx` used verbatim; ruling 1: kept the brief's full-width banner under the header and removed Task 21's smaller header button (the single, more visible instance); added one RTL test (`ExportMenu.test.tsx`) spying on `document.createElement` to assert the anchor's real `getAttribute("href")` (not `.href`, which jsdom resolves to an absolute URL) equals `api.exportUrl(...)`'s own output, and that it contains `format=hubspot`, `ids=` with both ids and `weights=` in R/E/D/B/S order; confirmed RED (temporarily asserted `format=csv`, saw it fail) then GREEN after revert; also stubbed the created anchor's `click()` since jsdom logs "Not implemented: navigation" on a real anchor click; a second test confirms both export buttons are disabled at `total=0` (used the DOM `.disabled` property, not a `toBeDisabled` jest-dom matcher — this repo has no jest-dom installed); verified live with `pnpm dev` + Playwright at 400/768/1280px by temporarily seeding 3 fake leads into `searchReducer.ts`'s `initialState` (`phase: "done"`) and one fake industry into `useBootstrap.ts`'s `industries` state (to exercise the search form's `canRun` gate with no live API) — confirmed no page-level horizontal scroll at any width while the results table scrolls inside its own card, the rail (`lg:grid-cols-[minmax(0,1fr)_320px]`) stacks under the table at 768px and sits beside it at 1280px, the drawer is ~385/400px wide and fully usable at 400px; full keyboard walkthrough confirmed Tab order banner-retry → industry → location → max-results → (disabled Scout skipped) → table select-all/sort buttons/rows/checkboxes → rail reset/presets/sliders → CSV/HubSpot CSV, Enter on a row opens the drawer with focus moving to the panel, Tab/Shift+Tab wrap correctly between the drawer's 3 focusable elements, and Escape returns focus to the triggering row; triggered a real `ApiError` by submitting the seeded form (network failure against the dev proxy) and confirmed the toast renders with `role="alert"` and auto-dismissed within one round of subsequent tool calls, consistent with the 8s timer; reverted both scratch edits afterward (`git diff` on `searchReducer.ts` and `useBootstrap.ts` came back empty); `pnpm build` then `VITE_API_URL=http://localhost:8000 pnpm build && pnpm preview` confirmed the built app loads and `grep`/network tab show requests going to `http://localhost:8000/healthz` and `/api/industries` (connection refused, no backend running, as expected); all 20 tests (18 existing + 2 new), typecheck and build pass; known pre-existing oxlint warning in `useSearchSession.ts` (missing `state.llmPending` dep, not touched by this task) plus one new oxlint warning on the brief's own prescribed `useEffect(() => { if (state.error) setToast(state.error); }, [state.error])` pattern (react/set-state-in-effect) — left as specified, not an error |
| 2026-09-18 | 17 intent + opener endpoints | 15 | 328 | measured; running total resumes the core API-budget lane from row 24 (313) — tasks 19-25 are the web track (Phase D, outside the 300-min core budget, hence their own "—" rows); brief's `routers/intent.py` and the opener route added to `routers/leads.py` used verbatim except `s: Session = Depends(get_session)` → `s: SessionDep` per controller ruling (repo convention, zero behavior change); all 4 new tests (RED confirmed: 404/module-missing, then GREEN) + 80/80 full suite pass (3 consecutive runs, 0 warnings), ruff check/format clean, no new `noqa` in `app/`; no bugs found in the brief's code; also fixed a pre-existing bug flagged by the coordinator once a real `GROQ_API_KEY` landed in `api/.env` (gitignored): `tests/test_health.py` deleted the env var instead of setting it empty, so pydantic-settings fell through to the dotenv file and `llm_enabled` came back `True`, failing `assert body["llm_enabled"] is False` — changed `monkeypatch.delenv("GROQ_API_KEY", raising=False)` to `monkeypatch.setenv("GROQ_API_KEY", "")` (an explicit empty env var beats the dotenv file, same precedent as `tests/conftest.py`'s module-level `GROQ_API_KEY=""`); RED confirmed with the real key present (`assert True is False`), GREEN after the fix, 80/80 full suite re-confirmed green (3 consecutive runs) with the real key in place |
| 2026-09-18 | 18 housekeeping, logging, Docker/Compose | 65 | 393 | measured, true total across the initial implementation (15) + fix round 1 (35) + fix round 2 (15) — Phase A done; brief's `services/housekeeping.py`, `logging_setup.py` used verbatim except one deviation per controller ruling 3 (no new `# noqa` in `app/`): dropped the brief's `# noqa: BLE001` on `run_daily`'s bare `except Exception` since `BLE` is not in this repo's ruff `select` list (`E, F, I, B, UP, ASYNC`), so the comment was dead weight; split the test's `s.add(old); s.add(new)` one-liner (ruling 3, E702) into two statements; RED confirmed (`ModuleNotFoundError: app.services.housekeeping`) then GREEN; wired `configure_logging`+`run_daily` into `main.py`'s lifespan verbatim; applied controller ruling 1 (live-validated finding) by reordering `config.py`'s default `groq_models` to `["qwen/qwen3.8-27b", "openai/gpt-oss-20b"]` (qwen first, gpt-oss kept as fallback) — checked `tests/test_config.py` and `tests/llm/*` for any order dependency, found none; applied ruling 2, quoting `CRAWLER_USER_AGENT` in `deploy/.env.example` so the unquoted `(...)` doesn't break `source`; Dockerfile/.dockerignore/compose files/Caddyfile/.env.example/docker-compose.local.yml all copied verbatim from the brief; full local Docker verification (Step 6) passed on the first attempt — `docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build` built and started postgres+api (caddy excluded via the `prod` profile), `curl localhost:8000/healthz` returned exactly `{"status":"ok","db":"ok","llm_enabled":false,"version":"dev"}`, `/api/industries` returned the industry list, `logs api --tail 30` showed alembic's own text-formatted `Running upgrade -> b1780eb594d6, initial` followed by the app's JSON `housekeeping.purged` line (alembic runs in its own `alembic upgrade head` process before uvicorn starts, so its own log lines predate `configure_logging` and are plain text by design — expected, not a bug) — first Postgres run of the migration surfaced no issues, the existing `JSONType = JSON().with_variant(JSONB(), "postgresql")` in `app/db.py` already handled the dialect switch; `down -v` cleaned up; confirmed `deploy/.env` is covered by the repo's existing root `.gitignore` (`.env`/`.env.*` patterns) via `git check-ignore -v`, no change needed; 81/81 full suite green (0 warnings, two separate runs), `ruff check .` and `ruff format --check .` clean. FIX ROUND 1 (commit `3f29c35`): added `package-data` to `pyproject.toml` (disposable_domains.txt was missing from site-packages, verified in the built image); logged unhandled 500s (`log.exception("request.failed", ...)` in both the middleware and the exception-handler backstop) and made `uvicorn`/`uvicorn.error`/`uvicorn.access` propagate into the root JSON handler instead of their own plain-text one, with a new `tests/test_main.py` (RED/GREEN via a temporary `git stash` of `main.py` — see round 2's process note below); reordered the Dockerfile to install dependencies before `COPY app` (measured 3m0.9s clean build vs 32.0s after a source-only edit, deps layer cached); fixed `.dockerignore` to `**/__pycache__`/`**/*.pyc`/`.env*` (reproduced the original leak — 7 pycache dirs — before fixing); made `purge_old` run via `asyncio.to_thread` off the event loop; strengthened `test_housekeeping.py` with `Signal`/`FactorScoreRow` rows and survivor assertions, proven to discriminate by injecting two bugs (a too-greedy delete and a wrong-id-selection bug) and reverting both afterward; added the `POSTGRES_PASSWORD` URL/configparser-safety comment to `.env.example`; full Docker re-verification passed with JSON `uvicorn.access` lines visible in the logs. FIX ROUND 2 (commit `5d47128`): added a SQLAlchemy `connect` event listener in `app/db.py` issuing `PRAGMA foreign_keys=ON` for SQLite only (Postgres already enforces), matching production semantics in dev/test; running the full suite with FK enforcement on surfaced 8 real pre-existing failures — 5 were test fixtures that added `Search`+`Lead` (and `Lead`+`Signal`) in the same session without an ORM `relationship()`, so SQLAlchemy's unit-of-work had no dependency edge to order the inserts and sometimes flushed the child first (fixed by adding `s.flush()` between parent and child adds in `tests/routers/test_searches.py` (4 spots) and `tests/routers/test_intent.py` (1 spot)); the other 3 (`tests/pipeline/test_runner.py`) traced to a **genuine latent production bug** in `app/pipeline/runner.py`'s `_process_candidate`: `s.add(lead)` followed immediately by `for ct in contacts: s.add(ct)` then a single `s.flush()` let Contact inserts race ahead of the Lead insert — harmless on SQLite with FK checks off, a real `ForeignKeyViolation` risk on Postgres in production. Fixed by flushing right after `s.add(lead)`. Proved the new pragma actually catches the FK-ordering bug it exists for: temporarily reordered `purge_old` to delete `Lead` before its children (edited in place, backed up first to a scratch path outside the repo per the no-`git stash`/no-`git checkout --` process rule), captured RED (`sqlite3.IntegrityError: FOREIGN KEY constraint failed` on `DELETE FROM leads`), then edited back and confirmed byte-identical to the backup before re-running GREEN. Full suite 82/82 green (0 warnings), ruff clean, no new `# noqa`. |
| 2026-09-19 | 26 server scripts, CI/CD, deploy workflow | 20 | 413 | measured; brief's `setup-ubuntu.sh` and `deploy.sh` used verbatim except one deviation per controller ruling 1: `deploy.sh` no longer `source`s `deploy/.env` (`set -a; . deploy/.env; set +a`) — replaced with a single `API_HOST="$(grep -E '^API_HOST=' deploy/.env | cut -d= -f2- | tr -d '"' | tr -d "'")"` since `.env` holds unquoted-`(...)`-hostile values like `CRAWLER_USER_AGENT` and `deploy.sh` needs exactly one variable from it (docker compose itself still auto-loads the rest of `deploy/.env` via `--project-directory`); `ci.yml`/`deploy.yml` copied verbatim from the brief; confirmed `ci.yml`'s `pip install -e ".[dev]"`/`ruff check .`/`ruff format --check .`/`pytest -q` match `api/pyproject.toml`'s `dev` extra and tool config, and `pnpm typecheck && pnpm test && pnpm build` match the exact script names in `web/package.json`; confirmed the `/healthz` grep pattern `"version": *"$sha"` matches the real (space-free, Starlette `separators=(",",":")`) JSON shape — corroborated by task 18's own logged `curl` output `{"status":"ok","db":"ok","llm_enabled":false,"version":"dev"}`; set the git-index executable bit on both scripts per ruling 2 (`git update-index --chmod=+x`, confirmed `100755` via `git ls-files -s`); confirmed LF endings on all 6 new/changed files via `git ls-files --eol` (all `i/lf w/lf`); validated both scripts with `bash -n` (syntax OK) and both workflow YAMLs with `python -c "import yaml; yaml.safe_load(...)"` (PyYAML 6.0.3 available on this machine's `python` — parses cleanly; note PyYAML's YAML-1.1 boolean quirk turns the `on:` key into `True` in the parsed dict, cosmetic only, GitHub's own parser is unaffected); per controller ruling 3, did **not** run the brief's Step 6 (`gh repo create`/`push`) — no GitHub repo created, no remote added, no push; work stops at a local commit for controller review. |
| 2026-09-19 | 26 (fix round 1) ref resolution + errexit-in-function + owner placeholder | 12 | 425 | measured; commit `7a63845`; review found 2 Important defects inherited from the brief's own `deploy_ref`, both reproduced empirically before fixing: (1) `git fetch --quiet origin` only advances remote-tracking refs, so `git checkout --quiet --detach "$ref"` resolved a branch name like `main` against the stale local `refs/heads/main` from clone time — reproduced with a throwaway bare `origin.git` + two clones (`work-origin`, `server`): pushed v1, cloned `server`, pushed v2 to origin, then on `server` after `git fetch`, `git checkout --detach main` landed on the old v1 sha while resolving `origin/main` first landed on the new v2 sha; fixed by resolving `origin/$ref^{commit}` first and falling back to `$ref^{commit}` (handles both branch names and shas, including the rollback path's already-fetched `PREV` sha) — the automated path (`deploy.yml` always passes a concrete sha) was unaffected, but the manual bootstrap command `setup-ubuntu.sh` itself prints (`./deploy.sh main`) was exactly the broken case; (2) `deploy_ref` was called as `if deploy_ref "$TARGET"; then`, and bash disables `errexit` for a function's entire body when the function is used as an `if`/`&&`/`||` condition, so a failed `git fetch`, `git checkout`, or `docker compose up --build` inside the function did not abort it — it would fall through, compute `sha` from an unchanged `HEAD`, and the health poll would then report a false "successful deploy" of a commit that was never actually deployed; reproduced with a minimal `set -euo pipefail` script where a function body's bare `false` let execution continue past it when called as an `if` condition, then showed an explicit `|| return 1` on the same line correctly aborts the function; fixed by adding `|| return 1` after `git fetch`, `git checkout`, and `docker compose ... up -d --build` inside `deploy_ref` rather than relying on `set -e` inside the function; (3, cosmetic) replaced the literal `<owner>` placeholder in `setup-ubuntu.sh`'s usage comment with `fasisandhu` in both the `curl` and clone URLs; re-verified `bash -n` clean on both scripts, `git ls-files -s deploy/` still `100755` for both, `git ls-files --eol` still `i/lf w/lf` for both; no repo created, no push, per controller ruling. |

| 2026-09-19 | deploy workflow guard (CI hygiene) | 5 | — | measured; the workflow_run deploy job fired on every green CI run and failed with "missing server host" because the Task 27 secrets do not exist yet, red-X'ing a repo whose code was fine; GitHub does not allow the `secrets` context in a job- or step-level `if`, so the fix reads DEPLOY_HOST as env in a first step and gates the ssh-action on that step's output, emitting a `::notice` when there is no VM to deploy to |
| 2026-09-19 | crawl bug found while sourcing eval pages | 15 | — | measured; not a planned task; selectolax maps an attribute that is present but empty to `None`, so `attributes.get(k, "")` never reaches its default — `<meta name="description" content="">` (Squarespace emits exactly that) and a bare `<a href>` both failed `PageText` validation inside `_page()`, and `crawl_site` turned that into `status="unreachable"`; the runner then recorded a reachable business as unreachable and dropped every website-derived contact and signal, quietly depressing its score; 1 of the 10 pages sampled from live OSM data hit it; RED confirmed with a regression test reproducing both crash sites, then fixed at all three `attributes.get` call sites in `clean_html`; 83/83 |
| 2026-09-19 | 28 extraction eval harness | 95 | — | measured — Phase D; sourced candidates by running the app's own geocode+Overpass stage over 6 industries in 6 US cities (47 OSM records carried a website tag; 2 of 8 Overpass queries failed outright, which is the documented mirror flakiness), fetched 16 homepages with the crawler's own user agent (2 sites 403'd that UA and 1 returned 502 — dropped rather than re-fetched behind a disguised UA), hand-labelled all four fields with a `note` per page recording the sentence each label rests on; ten pages came straight from discovery and six were added after probing for founding-year language, which is a deliberate selection bias disclosed in evals/README.md (the honest base rate is 1 of 10 randomly sampled pages stating a founding year); one of my own labels was WRONG on the first pass — tech-ridge-dental was labelled "no founding year, family ownership not stated" from its body text, the regex disagreed and returned 2011, and re-checking showed the page's own title and meta description say "Serving Austin since 2011" and "a local, family-owned dental office", so every label was then re-verified against title and meta as well as body; harness reports two views because `RegexSignals.family_owned` is a plain `bool` and cannot express "not stated", so strict exact-match would score a type signature rather than an extraction failure; results regex 31/64 exact and 54/64 positive-evidence, regex+LLM 41/64 and 58/64, with the entire exact gain coming from `is_chain` (1/16 → 8/16), a field the regex layer does not have |
| 2026-09-19 | 28 (follow-up) determinism + a negative result | 30 | — | measured; the eval immediately paid for itself twice; (1) `complete_json` never set `temperature`, so extraction ran at the provider default and reading the same page twice could differ — a prompt edit appeared to cost 3 points out of 64 until both arms were repeated at `temperature=0`, where the true difference was 1 changed judgement out of 64; fixed by threading `temperature` through `complete_json`/`_call` with a default of 0 and a test asserting the value reaches the client (the fake Groq stub now records kwargs); (2) writing a `description` for every extraction schema field, using the same wording as the ground truth, plus a system-prompt warning that customer reviews are not statements of fact, changed exactly one of 64 judgements and moved neither score — reverted, because it cost ~400 extra tokens on every call against an 8,000-token-per-minute free-tier budget and bought nothing measurable; both findings written up in evals/README.md rather than quietly dropped |
| 2026-09-19 | 29 notebook demo + sample dataset | 55 | — | measured — Phase D; notebook built from a generator script rather than hand-written .ipynb JSON so the cells stay reviewable; a separate venv (notebooks/requirements.txt) keeps pandas/matplotlib/jupyter out of the API's dependency list since this is an API client, not part of the service; found and fixed a real bug in my own SSE parser — `lead` carries the whole lead under `id` while `lead_updated` is a partial patch keyed on `lead_id` (confirmed intentional: web/src/state/searchReducer.ts destructures `lead_id` and merges the rest), and the first execution only passed because every page was cache-warm so zero `lead_updated` events ever arrived; re-executed against a third API instance on a genuinely cold database to capture the full lifecycle in the saved outputs (geocode 0.0s, discover 1.4s, enrich 6.0s, "Refining 12 leads with AI" 47.6s, done 137.5s with 54 `lead` + 11 `lead_updated` events, then 4 polls until llm_pending hit 0); the notebook recomputes the app's client-side re-ranking in pandas and asserts it against the server's stored score — max disagreement 0.00 points across 54 leads, the same contract rank.ts is tested on; MEASURED CACHING NUMBER for the README: the identical query is 137.5s cold and 36.2s warm; sample dataset exported from that same cold run (54 leads, tiers A7/B4/C27/D16, 18 with a website, 12 crawled ok, 7 MX-verified emails, 12 LLM-refined) in both CSV shapes, BOM present on the generic file and absent on the HubSpot one as designed, with data/README.md recording provenance, contact-data ethics and the ODbL attribution |
| 2026-09-19 | 31 (part) nightly backup script | 10 | — | measured — Phase D; `deploy/backup.sh` dumps Postgres, gzips, verifies and keeps the last 7; deviates from the plan's four-liner in one way that matters: it writes to a `.partial` name and renames only after `gzip -t` passes, because the plan's version would leave a truncated archive on a dump killed halfway (disk full, container restarting) that still looks like a good backup and still counts as one of the seven retained, so a week of those would silently destroy every real backup; `bash -n` clean, git index mode 100755, LF |
| 2026-09-19 | 30 (part) architecture, runbook, video script | 60 | — | measured — Phase D; `docs/architecture.md` carries the ASCII overview, a Mermaid sequence diagram of one search including the post-`done` refinement loop, the full SSE event contract table (explicitly naming the `id` vs `lead_id` asymmetry that bit my own notebook), and a per-module map for `api/app`, `api/app/pipeline`, `api/app/llm` and `web/src`; `docs/runbook.md` gives every operation as a full copy-pasteable command against `/opt/squatchscout/deploy` and documents real failure modes rather than the happy path (HTTPS failing because port 80 is shut even though the site serves on 443, a Docker build dying silently to the OOM reaper on a 1 GB box, why Alembic deliberately bypasses the SQLite FK pragma, what `llm_status: skipped_budget` means and why the table is still correct when it happens, why a Vercel preview origin will always fail CORS); `docs/video-script.md` is 6 shots at ~265 words with a before-recording checklist, a table mapping every spoken number to the file it comes from, and a "what not to say" list (do not claim we find personal contact details, do not describe the model as deciding anything, do not present 16 hand-labelled pages as a benchmark); README deliberately NOT written yet — almost every one of its 17 sections cites the deployed URLs, measured live latency or the health endpoint, so writing it before Task 27 would mean writing claims I cannot verify |
| 2026-09-19 | 27 first deployment (EC2, TLS, Vercel, verification) | 50 | — | measured — Phase C closes here; AWS CLI 2.36.49 installed and signed in with `aws login` (federates a console session into short-lived credentials, so no long-lived access key ever touched disk); built with the CLI rather than the console so every step is reproducible: newest Canonical Ubuntu 24.04 AMI (ami-025d99823a4caad37), ed25519 key imported, security group sg-092726627c947d228, t3.micro with a 16 GB gp3 root and IMDSv2 required, Elastic IP 100.51.99.130, hostname api.100-51-99-130.sslip.io; `setup-ubuntu.sh` and `deploy.sh main` both worked first time and Caddy obtained the certificate on the first request; confirmed the AMI already ships `PasswordAuthentication no`; DEVIATION FROM THE PLAN, flagged: port 22 is restricted to the operator IP rather than opened to the world, because the GitHub Actions deploy runs from ephemeral runner IPs and opening SSH to 0.0.0.0/0 to accommodate that is a real weakening — auto-deploy is therefore not yet wired and the workflow skips cleanly (see the DEPLOY_HOST guard); Vercel project linked with root `web/`, `VITE_API_URL` set, production URL https://squatchscout-omega.vercel.app (not the guessed squatchscout.vercel.app, so FRONTEND_ORIGINS/PUBLIC_URL/CRAWLER_USER_AGENT were corrected on the server and the api container recreated); verified end to end: /healthz reports the deployed sha, HTTP 308s to HTTPS, CORS preflight returns the exact Vercel origin, and the bundle has the API URL baked in |
| 2026-09-19 | production-only bug: osm_id overflow | 25 | — | measured; the FIRST real production search exposed a defect no test could have caught on SQLite: `leads.osm_id` was declared `sa.Integer()`, but OpenStreetMap node ids passed 2^31 years ago, so every candidate above 2,147,483,647 raised `psycopg.errors.NumericValueOutOfRange` on flush; Task 14's per-lead guard caught each one and logged `lead.failed`, so the search reported `status: done` with `lead_count: 54` while persisting exactly 5 leads — a silent 91% data loss that looked like success from the outside; found by running the demo query against production and noticing it finished in 9s with 5 rows instead of ~140s with 54; fixed by declaring `osm_id` as `BigInteger` plus Alembic migration 0002 using `batch_alter_table` so it also applies on SQLite; the regression test asserts the DECLARED COLUMN TYPE rather than round-tripping a large value, because SQLite stores any integer width happily and a value-based test would pass whether or not the bug is present — which is exactly how this reached production in the first place; 85/85, redeployed, and the same query now returns 54 persisted leads in 92s with 12 AI refinements |
| 2026-09-19 | 30 (rest) screenshots + README | 45 | — | measured — Phase D; three screenshots captured from the DEPLOYED app via Playwright at 1440x900 (empty state, results table with the succession preset active and the weights rail, lead drawer with factor bars/contacts/provenance pills); the planned streaming GIF was skipped — the owner records a screen capture for the video and a second recording of the same thing adds nothing; README written against what is actually running rather than from memory: every internal link resolved, both live URLs returned 200, the dependency versions copied out of `api/pyproject.toml` and `web/package.json`, the eval table copied out of `evals/results.md`, the coverage numbers (18 of 54 OSM records carried a website, 7 MX-verified emails, 16 reached the model) read back off the production API, and the caching pair re-measured on the deployed instance (92s cold, 4s warm — my first draft said "under 10 seconds" from memory and the real figure is 4, so the text was corrected); states two uncomfortable things plainly instead of burying them — auto-deploy is off because enabling it as designed means opening port 22 to the internet, and the build took ~15h against the handbook's 5h guideline |
| 2026-09-19 | 31 (rest) final hygiene gate | 10 | — | measured; 0 TODO/FIXME/XXX in `api/app` and `web/src`, 0 `Co-Authored-By` and 0 `Claude-Session` trailers across all 67 commits, 0 tracked `.env`, 0 tracked `.docx`, 0 files matching secret-shaped patterns (gsk_/AKIA/PRIVATE KEY), 174 tracked files; did NOT re-run the plan's clean-clone build step because CI already does exactly that on every push from a fresh checkout (pip install -e ".[dev]", ruff check + format --check, pytest; pnpm install --frozen-lockfile, typecheck, test, build) and duplicating it locally would verify nothing new |
